{{ form_start(form) }}
<h4>Êtes-vous sûr de vouloir supprimer {% if page.type === 'menu' %}le menu{% else%}la page{% endif %}"{{ page.title }}" ?</h4>
    <button type="submit" class="btn btn-danger">Supprimer</button>
{{ form_end(form) }}